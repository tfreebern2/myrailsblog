<br />
<br />
<div class="col-xs-10 col-xs-offset-1 text-center">
  <div class="row">
    <div class="col-sm-12">
      <h1>Stuff I've Written</h1>
      <hr class="center-block">
      <% @posts.each do |post|%>
        <h3><%= link_to post.title, post %></h3>
        <p class='blog-date'><%= post.created_at.strftime("%A, %b %d") %></p>
        <hr class="center-block">
      <% end %>
      <%= will_paginate @posts %>
    </div>
  </div>
</div>